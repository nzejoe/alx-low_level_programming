#include <stdio.h>
/**
 * main - print string to the screen with printf
 *
 * Return: 0 (Does not return anything than zero (0))
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
